# Configuration NPM pour éviter les warnings peer dependencies
legacy-peer-deps=true

# Supprimer les warnings de packages deprecated (sauf critiques)
audit-level=high

# Accélérer l'installation
prefer-offline=true

